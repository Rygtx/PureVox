# Windows 专属依赖：PyInstaller 打包工具链 + win32 系统服务（firewall/registry/runas）
# 用法: pip install -r requirements.txt -r requirements-win.txt
# 注意：打包 EXE 在 Win7 上 "DLL load failed while importing QtWidgets: 找不到指定的模块"
# 的根因是 build_win.ps1 第 4 步瘦身误删 Qt6Qml.dll（pyside6.abi3.dll 硬依赖它），
# 与 pyinstaller/hooks-contrib 版本无关（2026-08-09 QEMU Win7 pefile + 实机复现确认；
# 曾误以为锁 6.21.0/2026.6 能解决，实测不能，勿再按此方向锁版本）。
altgraph
pefile
pyinstaller
pyinstaller-hooks-contrib
pywin32
pywin32-ctypes

# PyAudio（从 requirements.txt 移来）：仅 Windows/macOS 后端使用（diagnose_wasapi.py、
# audio_processor.py 的 WASAPI/PortAudio 路径）；Linux 走原生 PipeWire 不需要它，
# 放这里避免 Linux 建环境时编译失败（缺 portaudio.h）。
PyAudio
