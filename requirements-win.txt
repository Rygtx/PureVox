# Windows 专属依赖：PyInstaller 打包工具链 + win32 系统服务（firewall/registry/runas）
# 用法: pip install -r requirements.txt -r requirements-win.txt
# pyinstaller / hooks-contrib 必须锁 Win7 实测版本（2026-08-09）：最新 hooks-contrib 收集
# 老版 PySide6 6.1.3 的 Qt6 DLL 不完整 → 打包 EXE 在 Win7 上 "DLL load failed while
# importing QtWidgets: 找不到指定的模块"；源码直跑无此问题。6.21.0 + 2026.6 实测可用。
altgraph==0.17.5
pefile==2024.8.26
pyinstaller==6.21.0
pyinstaller-hooks-contrib==2026.6
pywin32
pywin32-ctypes
