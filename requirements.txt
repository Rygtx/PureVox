# Python 3.12+（已迁移自 3.8，Win7 支持终止；3.13 暂无 PyAudio wheel，CI 与内置均切到 3.12）
# 跨平台运行依赖（Windows / Linux / macOS 通用）
#
# 音频引擎为纯 Python（pvengine 包）：numpy 做帧级 DSP，onnxruntime 跑模型推理，
# scipy 提供 EQ 双二阶层联的 lfilter。
# 版本策略（2026-08-25 起 pin ==本机验证版本）：CI 与本地必须同一套依赖，
# 避免产物行为漂移（首启托盘问题排查期间确认 CI 产物与本地不可复现差异的
# 主要嫌疑就是不锁版本）；升级 = 本机验证后改这里。
importlib_metadata
packaging
protobuf
numpy==2.5.2
scipy==1.18.0
onnxruntime==1.29.0
# PyAudio 仅 Windows 后端使用（Linux 走 pipewire-pulse 桥，见 audio_processor.py）；
# 环境标记跳过，Linux 建环境不再尝试编译（缺 portaudio.h 会失败）
cryptography>=41.0.0
zeroconf==0.150.0
aiohttp==3.14.3
opuslib==3.0.1
zipp
# Linux 音频桥：ctypes 到系统 libpulse（pipewire-pulse 兼容层），无自编译组件；
# 仅 Linux 运行需要，装了也无害
pulsectl

# ── 媒体解码/网络组件 ──
# websockets = Lite Net WSS 服务器（仅 Windows 发行）；
# av(PyAV) = 选曲一次性转码（miniaudio 不支持的格式 → .purevox.wav）
#   + Lite Net 的 Opus 解码，全平台安装；
# qrcode = 连接二维码（仅 Windows 发行）。
# 环境标记让 Linux 解析时跳过 Windows 专用项，两条 Windows 工作流
# （主线 / Lite）轮子集与缓存键保持完全一致，pip 缓存桶直接互通
websockets==17.0.1; sys_platform == "win32"
av==18.1.0
qrcode==8.2; sys_platform == "win32"

# 媒体运行时解码/播放唯一实现（音效板/音乐播放器/纯媒体会话）：
# mp3/flac/ogg/wav，自包含 C，跨平台 wheel，无系统依赖
miniaudio==1.71

# ── Windows 平台件（打包工具链 + 系统服务绑定），标记限定单平台 ──
altgraph; sys_platform == "win32"
pefile; sys_platform == "win32"
pyinstaller==6.22.2; sys_platform == "win32"
pyinstaller-hooks-contrib==2026.6; sys_platform == "win32"
pywin32; sys_platform == "win32"
pywin32-ctypes; sys_platform == "win32"
PyAudio==0.2.14; sys_platform == "win32"

# 全平台单文件：平台差异一律走环境标记（pip 官方机制），
# 不再维护独立 requirements-win.txt
