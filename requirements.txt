# Python 3.8+（Windows 7 需 Python 3.8，最后一个支持 Win7 的 Python）
# 跨平台运行依赖（Windows / Linux / macOS 通用）
flatbuffers
importlib_metadata
# opset：v9=13、tse15=14、aec9=15（原 17 已降级，LayerNormalization 展开为基础算子）
# 全部模型 opset ≤15，旧 onnxruntime（1.11+）理论上可加载；下限按已实测的 1.13 声明
# py3.8 下 pip 会解析到最后一个带 cp38 wheel 的版本（约 1.19.x）
onnxruntime>=1.13
packaging
protobuf
PyAudio
pybind11
# PySide6 6.x 需 Windows 10+；py3.8 下 pip 取 6.7.x（最后一个 cp38 wheel）
PySide6>=6.5.0
cryptography>=41.0.0
zeroconf
aiohttp>=3.9.0
opuslib>=3.0
zipp

# Windows 专属依赖见 requirements-win.txt（pip install -r requirements.txt -r requirements-win.txt）
