# Python 3.12+（已迁移自 3.8，Win7 支持终止；3.13 暂无 PyAudio wheel，CI 与内置均切到 3.12）
# 跨平台运行依赖（Windows / Linux / macOS 通用）
importlib_metadata
packaging
protobuf
# PyAudio 仅 Windows/macOS 后端使用（Linux 走原生 PipeWire，见 audio_processor.py），
# 放在 requirements-win.txt，避免 Linux 建环境时编译它（缺 portaudio.h 会失败）。
# opset：v9=13、tse15=14、aec9=15 —— 全部模型 opset ≤18（ONNX Runtime 1.22 支持 opset 22）
# 推理用纯 C 的 ONNX Runtime（aimic.c 链接 libonnxruntime.so，ctypes/CDLL 加载，
# 不做 Python 层 onnx/onnxruntime）：Linux 捆绑 packages/onnxruntime-linux-x64-1.22.0
# （lib/libonnxruntime.so*），Windows 捆绑 packages/onnxruntime-win-x64-1.22.0（onnxruntime.dll）。
# 注意：这里的 PySide6 是 GUI 栈依赖；onnxruntime 不经过 pip，见 setup.py 注释。
# 纯 C 引擎（aimic.c / pipewire_client.c）由 gcc 直接构建，ctypes 加载，无 pybind11
# 编译器依赖：Linux 用 pkg-config + libpipewire-0.3-dev + gcc
# 版本下限对齐当前最新发布（2026-08-22 核对）：CI 无锁版，全新环境即装最新；
# 唯一例外 onnxruntime==1.22.0 由构建脚本锁定——C 引擎捆绑的 SDK 就是 1.22.0，
# 升级需同步重捆 packages/onnxruntime-*，勿单独改
PySide6>=6.11
cryptography>=50.0.0
zeroconf>=0.150
aiohttp>=3.14
opuslib>=3.0.1
zipp

# Windows 专属依赖见 requirements-win.txt（pip install -r requirements.txt -r requirements-win.txt）
